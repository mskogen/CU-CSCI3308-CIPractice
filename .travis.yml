i# Matt Skogen & Isaiah Jones

language: c

script: 
  - make dep
  - make test

install: sudo apt-get install check

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake